services:
  sticker-mcp:
    build: .
    container_name: sticker-mcp
    restart: unless-stopped
    env_file: .env
    environment:
      - PORT=3000
      - DATA_DIR=/app/data
    ports:
      - "127.0.0.1:3000:3000"
    volumes:
      - sticker_data:/app/data

volumes:
  sticker_data:
